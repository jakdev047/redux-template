const initialState = {
    appName: "Redux App",
};

const appInfoReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default appInfoReducer;
